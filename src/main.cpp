#include <iostream>
#include "Engine.h"
#include "Algorithms.h"

int main() {
    Engine* engine;
    int sq = E4;
    int64 a = 1;

    engine = new Engine();
    engine->run();
    //delete engine;

    return 0;
}
